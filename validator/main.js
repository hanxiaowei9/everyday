
// 选中页面中所有的input[data-rule]
// 解析每个input的验证规则
//验证
var validator = new Validator("15",{
    max:100,
    min:18,
});

$(
    function(){
        "use strict";

        window.Validator = function(val,rule){
       val 
        }
    }
)